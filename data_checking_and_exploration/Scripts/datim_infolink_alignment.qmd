---
title: "DATIM / Infolink Alignment"
author: "Aditi Arunmozhi"
format: html
editor: visual
---

Set Up

```{r setup}
library(tidyverse)
library(glamr)
```

Reading in files

```{r}
setwd("C:/Users/STAR/Documents/Git/KP_MSD/data_checking_and_exploration/Data")
infolink_data <- read_csv("Infolink_Tanzania.csv")
datim_data <- read_csv("DATIM_Tanzania.csv")
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

```{r}

```
